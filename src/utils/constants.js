// Theme colors
export const COLORS = {
  primary: "#4a7c59",
  primaryRgba: "rgba(74, 124, 89, 0.2)",
  primaryDark: "#2d5a3d",
  textLight: "#e0e0e0",
  textDark: "#2c3e50",
  gridLight: "rgba(0, 0, 0, 0.1)",
  gridDark: "rgba(255, 255, 255, 0.1)",
};

// Map zoom configuration
export const ZOOM_CONFIG = {
  extent: 1.3,
  duration: 1500,
  easing: "ease-in-out",
};

// Search configuration
export const SEARCH_CONFIG = {
  maxSuggestions: 5,
};

// LocalStorage keys
export const STORAGE_KEYS = {
  theme: "populationAppTheme",
};
